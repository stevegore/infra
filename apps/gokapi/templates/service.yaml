apiVersion: v1
kind: Service
metadata:
  name: {{ include "gokapi.fullname" . }}
  labels:
    {{- include "gokapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gokapi.selectorLabels" . | nindent 4 }}
