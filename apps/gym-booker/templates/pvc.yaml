apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "gym-booker.fullname" . }}-data
  labels:
    {{- include "gym-booker.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.persistence.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
