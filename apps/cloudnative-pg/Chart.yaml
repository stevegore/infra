apiVersion: v2
name: cloudnative-pg
description: CloudNativePG operator — cluster-wide PostgreSQL operator (CRDs + controller). Shared infra; apps declare their own Cluster/Database CRs against it.
type: application
version: 1.0.0
appVersion: "1.29.1"

dependencies:
  - name: cloudnative-pg
    version: 0.28.2
    repository: https://cloudnative-pg.github.io/charts
