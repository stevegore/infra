apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "garmin-mcp.fullname" . }}-data
  labels:
    {{- include "garmin-mcp.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.persistence.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
