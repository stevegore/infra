apiVersion: v1
kind: Service
metadata:
  name: {{ include "strava-keeper.fullname" . }}
  labels:
    {{- include "strava-keeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "strava-keeper.selectorLabels" . | nindent 4 }}
