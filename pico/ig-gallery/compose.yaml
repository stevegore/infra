services:
  ig-gallery:
    image: nginx:1.31.3-alpine@sha256:4a73073bd557c65b759505da037898b61f1be6cbcc3c2c3aeac22d2a470c1752
    container_name: ig-gallery
    restart: unless-stopped
    ports:
      - "8090:80"
    volumes:
      - /media/m2/ig-gallery:/usr/share/nginx/html:ro
      - /media/m2/ig-gallery-state:/state:ro
      - /media/m2/ig-gallery-admin/nginx.conf.template:/etc/nginx/templates/default.conf.template:ro
    environment:
      TZ: Australia/Sydney
      NGINX_ENVSUBST_FILTER: "^GALLERY_"
      GALLERY_ADMIN_KEY: ${GALLERY_ADMIN_KEY}
      # Home public IP as Caddy sees it. Kept out of the repo (residential,
      # dynamic) — set it in the Portainer stack environment. The TEST-NET-3
      # fallback is never a real client, so an unset value degrades to
      # "no public-IP admin access" rather than breaking the nginx template.
      GALLERY_ADMIN_IP: ${GALLERY_ADMIN_IP:-203.0.113.4}
    depends_on:
      - gallery-admin
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/index.html"]
      interval: 30s
      timeout: 5s
      retries: 3

  gallery-admin:
    image: python:3.14.7-alpine@sha256:f2186fc449b8f7aa5897b542777427a21dc77864f271cf4d1646361cf681c2b9
    container_name: gallery-admin
    restart: unless-stopped
    user: "1000:1000"
    volumes:
      - /media/m2/ig-gallery-admin/gallery-admin.py:/app/gallery-admin.py:ro
      - /media/m2/ig-gallery-state:/state
    environment:
      TZ: Australia/Sydney
      GALLERY_STATE_DIR: /state
      PYTHONUNBUFFERED: "1"
    command: ["python3", "/app/gallery-admin.py"]
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/healthz', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
